import { createFileRoute,Link } from "@tanstack/react-router";import { useState,type FormEvent } from "react";import { supabase } from "@/integrations/supabase/client";import { Brand } from "@/components/brand";import { Button } from "@/components/ui/button";import { Input } from "@/components/ui/input";import { Label } from "@/components/ui/label";
export const Route=createFileRoute("/reset-password")({head:()=>({meta:[{title:"Reset password — EARNPESA"},{name:"description",content:"Securely set a new EARNPESA password."},{property:"og:title",content:"Reset password — EARNPESA"},{property:"og:description",content:"Secure account recovery."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary"}]}),component:Page});
function Page(){const [msg,setMsg]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();if(!window.location.hash.includes("type=recovery")){setMsg("Open the recovery link from your email first.");return;}const p=String(new FormData(e.currentTarget).get("password")??"");if(p.length<8){setMsg("Use at least 8 characters.");return;}const {error}=await supabase.auth.updateUser({password:p});setMsg(error?.message??"Password updated. You can now log in.");}return <main className="grid min-h-screen place-items-center bg-muted p-5"><div className="premium-shadow w-full max-w-md rounded-lg border bg-card p-7"><Brand/><h1 className="mt-10 text-3xl font-bold">Set a new password</h1><form onSubmit={submit} className="mt-7 space-y-4"><div><Label htmlFor="password">New password</Label><Input id="password" name="password" type="password" className="mt-2 h-11" minLength={8} maxLength={72} required/></div><Button className="w-full">Update password</Button></form>{msg&&<p className="mt-4 text-sm">{msg}</p>}<Link to="/auth" search={{mode:"login"}} className="mt-5 block text-sm font-semibold text-primary">Return to login</Link></div></main>}