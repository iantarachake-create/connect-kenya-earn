import { Link,useNavigate } from "@tanstack/react-router";import { BarChart3,Bell,BriefcaseBusiness,Gift,LayoutDashboard,LogOut,Menu,Settings,Share2,ShieldCheck,WalletCards } from "lucide-react";import type {ReactNode} from "react";import { useQueryClient } from "@tanstack/react-query";import { Brand } from "./brand";import { Button } from "./ui/button";import { supabase } from "@/integrations/supabase/client";
const nav=[["/dashboard",LayoutDashboard,"Dashboard"],["/earn",Gift,"Earn"],["/referrals",Share2,"Referrals"],["/wallet",WalletCards,"Wallet"],["/business",BriefcaseBusiness,"Business"],["/admin",ShieldCheck,"Admin"]] as const;
export function AppShell({title,children}: {title:string;children:ReactNode}){const q=useQueryClient(),navigate=useNavigate();async function logout(){await q.cancelQueries();q.clear();await supabase.auth.signOut();navigate({to:"/auth",search:{mode:"login"},replace:true});}return <div className="min-h-screen bg-muted/50"><aside className="fixed inset-y-0 left-0 z-40 hidden w-64 border-r bg-card p-5 lg:block"><Brand/><nav className="mt-10 space-y-1">{nav.map(([to,Icon,label])=><Link key={to} to={to} className="flex items-center gap-3 rounded-md px-3 py-2.5 text-sm font-semibold text-muted-foreground hover:bg-muted hover:text-foreground" activeProps={{className:"bg-secondary text-primary"}}><Icon className="size-4"/>{label}</Link>)}</nav><Button variant="ghost" className="absolute bottom-5 left-5 justify-start" onClick={logout}><LogOut/> Sign out</Button></aside><div className="lg:pl-64"><header className="sticky top-0 z-30 flex h-16 items-center justify-between border-b bg-background px-4 sm:px-7"><div className="flex items-center gap-3"><Button variant="ghost" size="icon" className="lg:hidden"><Menu/></Button><h1 className="text-lg font-bold">{title}</h1></div><div className="flex items-center gap-1"><Button variant="ghost" size="icon" aria-label="Notifications"><Bell/></Button><Button variant="ghost" size="icon" aria-label="Settings"><Settings/></Button><div className="ml-2 grid size-9 place-items-center rounded-full bg-secondary text-xs font-bold">KM</div></div></header><main className="mx-auto max-w-7xl p-4 pb-24 sm:p-7 lg:pb-8">{children}</main></div><nav className="fixed inset-x-0 bottom-0 z-50 grid grid-cols-4 border-t bg-card px-2 py-2 lg:hidden">{nav.slice(0,4).map(([to,Icon,label])=><Link key={to} to={to} className="flex flex-col items-center gap-1 py-1 text-[11px] font-semibold text-muted-foreground" activeProps={{className:"text-primary"}}><Icon className="size-5"/>{label}</Link>)}</nav></div>}